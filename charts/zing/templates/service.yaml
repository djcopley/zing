apiVersion: v1
kind: Service
metadata:
  name: {{ include "zing.fullname" . }}
  labels:
    {{- include "zing.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zing.selectorLabels" . | nindent 4 }}
